#!/bin/bash

for file in $(ls -d m_*.f90); do
	#echo $file;
	~/refac/refac/change_size_array.py $file MCENT atom ncent_tot -ow
	# ~/refac/refac/clean_use.py $file -ow
	# cd ../../build
	# make
	# ([ $? -eq 0 ] && echo "success") || exit $?
	# cd ../src/vmc
done

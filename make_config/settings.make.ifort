  FC = ifort
# Warning: do no use of r8 option, conflict with orbitals on a grid declarations
  COMMON_FLAGS = -O2 -xHost -mcmodel=large -shared-intel \
    -dyncom="grid3d_data,orbital_num_spl,orbital_num_lag,orbital_num_spl2,grid3d_data"
# COMMON_FLAGS = -check uninit -check format 
  FFLAGS = $(COMMON_FLAGS) -extend-source 132 -fixed 
  F90FLAGS = $(COMMON_FLAGS) 
  FC_MPI = mpiifort
  FFLAGS_MPI = $(COMMON_FLAGS) -extend-source 132 -fixed

  PERIODIC = F
  QMMM = T
